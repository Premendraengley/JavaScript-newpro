//object
const user = {
    name : "prince",
    branch: "CSE",
    college : "JIT"
}

//simple calling
console.log(user.name);

//de-sturcturing the object
//we can also de-sturcturing  the arrays
const {name : n1} = user
console.log(n1);s